---
title: "Create Campaign"
description: "Create a new WhatsApp campaign shell without recipients"
openapi: "POST /whatsapp/campaigns"
---

<Note>
This endpoint creates the campaign shell. You must add recipients using the [Attach Recipients](/api-reference/whatsapp/campaigns/attach-recipients) endpoint before starting the campaign.
</Note>